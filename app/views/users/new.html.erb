<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cadastro - Camaar</title>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
</head>
<body>
    <div class="login-container">
        <h1>Cadastro</h1>
        <%= form_with model: @user, url: signup_path, method: :post, class: "login-form" do |form| %>
        <div class="form-group">
            <%= form.label :name, "Nome Completo" %>
            <%= form.text_field :name, id: "name", placeholder: "Digite seu nome", required: true %>
        </div>
        <div class="form-group">
            <%= form.label :email, "Email" %>
            <%= form.email_field :email, id: "email", placeholder: "Digite seu email", required: true %>
        </div>
        <div class="form-group">
            <%= form.label :password, "Senha" %>
            <%= form.password_field :password, id: "password", placeholder: "Digite sua senha", required: true %>
        </div>
        <div class="form-group">
            <%= form.label :password_confirmation, "Confirme sua senha" %>
            <%= form.password_field :password_confirmation, id: "password_confirmation", placeholder: "Confirme sua senha", required: true %>
        </div>
        <%= form.submit "Cadastrar", class: "login-button" %>
        <% if flash[:alert] %>
            <div class="alert alert-alert"><%= flash[:alert] %></div>
        <% end %>
        <% end %>
        <p>Já tem uma conta? <a href="<%= login_path %>">Faça login</a></p>
    </div>
</body>
</html>
